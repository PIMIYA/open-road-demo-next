import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: "#ffe4db"
    },
    primary: {
      main: "#000",
    },
    secondary: {
      main: "#3035a4",
    },
  }
});

export default theme;
